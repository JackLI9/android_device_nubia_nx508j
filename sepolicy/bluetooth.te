#allow bluetooth zygote:netlink_selinux_socket getopt;
allow bluetooth_loader btnvtool_exec:file { read open execute getattr execute_no_trans };
allow bluetooth_loader smem_log_device:chr_file { read open write ioctl };
allow bluetooth_loader self:capability { dac_override };
